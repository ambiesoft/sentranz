// pub fn split_sentences(text: &str) -> Vec<String> {
//     text.split('.')
//         .map(|s| s.trim())
//         .filter(|s| !s.is_empty())
//         .map(|s| format!("{}.", s))
//         .collect()
// }
